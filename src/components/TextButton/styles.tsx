import styled from "styled-components"

export const Container = styled.button`
    background: none;
    border: none;

    color: var(--white);

    cursor: pointer;

    a{
        text-decoration: none;
        color: var(--white);
    }

`